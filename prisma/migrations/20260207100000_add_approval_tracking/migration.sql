-- This migration is empty - no schema changes needed
-- The approval workflow uses existing fields:
-- - marriageDate stores Kazi approval date (when approvalStatus = 'checked')
-- - approvalDate stores Admin approval date (when approvalStatus = 'approved')

